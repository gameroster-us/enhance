<%= form_for(@job) do |f| %>
  <% if @job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% @job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :number_of_capacity %><br>
    <%= f.text_field :number_of_capacity %>
  </div>
  <div class="field">
    <%= f.label :employee_ids, "Employee" %><br>
    <%= select_tag 'job[employee_ids][]', options_for_select(Employee.all.pluck(:name, :id), {prompt: "Select Employee", selected: @job.employees.ids}), {class: "form-control form-control-sm", multiple: true} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
